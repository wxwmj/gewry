name: Auto Update Sub

on:
  schedule:
    - cron: '0 22 * * *'  # 北京时间 06:00
    - cron: '0 10 * * *'  # 北京时间 18:00
  workflow_dispatch:

jobs:
  update-sub:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: 🔧 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: 📦 Install dependencies
        run: |
          pip install -r requirements.txt
          pip install aiohttp  # 安装额外的依赖 aiohttp

      - name: 📂 Create output folder (if not exists)
        run: mkdir -p output  # 创建 output 文件夹

      - name: 🚀 Run Node Checker
        run: python node_checker.py

      - name: 📤 Commit and push updated sub
        env:
          PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          # 检查 output 文件夹是否有更新
          if [ "$(find output -type f | wc -l)" -gt 0 ]; then
            # 如果 output 文件夹有文件
            git config --global user.email "github-actions[bot]@users.noreply.github.com"
            git config --global user.name "github-actions[bot]"
            git remote set-url origin https://x-access-token:${PAT_TOKEN}@github.com/${{ github.repository }}.git
            git add output
            git commit -m "🤖 自动更新 sub 文件 $(date '+%Y-%m-%d %H:%M:%S')" || echo "无变化无需提交"
            git push origin HEAD:${{ github.ref_name }}
          else
            echo "📦 没有生成新的订阅文件，跳过提交"
          fi
